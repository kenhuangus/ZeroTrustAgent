# LLM Configuration
# Priority order for LLM providers:
# 1. Gemini (if API key available)
# 2. Local Ollama (if available)
# 3. OpenAI (if API key available)

# Google Gemini configuration
GOOGLE_API_KEY=your_api_key_here

# OpenAI API configuration (used as fallback)
OPENAI_API_KEY=your_api_key_here

OGETHERAI_API_KEY=your_api_key_here

# Force specific LLM provider (optional)
# Options: "gemini", "ollama", "openai"
FORCE_LLM_PROVIDER=